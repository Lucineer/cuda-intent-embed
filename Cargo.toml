[package]
name = "cuda-intent-embed"
version = "0.1.0"
edition = "2021"
description = "Intent embeddings — human text to structured intent vectors for A2A deliberation"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
